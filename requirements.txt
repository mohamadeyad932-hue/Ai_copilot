# ===============================================
# متطلبات المشروع (Project Requirements)
# ===============================================

# المشروع يعتمد بشكل أساسي على مكتبات بايثون القياسية (json, pathlib, typing, os).
# فيما يلي حزم إضافية مفيدة للتطوير والتشغيل:

# للتعامل مع متغيرات البيئة (اختياري)
python-dotenv>=1.0.0

# لتنسيق وفحص الكود (اختياري)
flake8>=6.0.0
black>=23.0.0

# للاختبارات (اختياري)
pytest>=7.0.0
